---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: bifrost-staging-pods-reader
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: bifrost-staging-pods-reader
subjects:
  - kind: ServiceAccount
    name: bifrost-staging-ksa
    namespace: bifrost-staging
roleRef:
  kind: ClusterRole
  name: bifrost-staging-pods-reader
  apiGroup: rbac.authorization.k8s.io
---
# Cluster-scoped (not a Role in the argocd namespace) because the
# kustomization's global `namespace:` transformer would rewrite a namespaced
# Role/RoleBinding into bifrost-staging, silently breaking promote RBAC.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: bifrost-staging-argocd-app-patcher
rules:
  - apiGroups: ["argoproj.io"]
    resources: ["applications"]
    verbs: ["get", "list", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: bifrost-staging-argocd-app-patcher
subjects:
  - kind: ServiceAccount
    name: bifrost-staging-ksa
    namespace: bifrost-staging
roleRef:
  kind: ClusterRole
  name: bifrost-staging-argocd-app-patcher
  apiGroup: rbac.authorization.k8s.io
